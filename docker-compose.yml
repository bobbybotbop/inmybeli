services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5001:5001"
    env_file:
      - backend/.env
    volumes:
      - backend_instance:/app/backend/instance

volumes:
  backend_instance: